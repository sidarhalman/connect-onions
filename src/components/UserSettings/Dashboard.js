import React from 'react';

const Dashboard = () => {
    return <h2>Dashboard</h2>

    // after clicking on submit 
    // user should be redirected to his dashboard 
    // either the form or button in signup component should have the function to show the dashboard


}

export default Dashboard;